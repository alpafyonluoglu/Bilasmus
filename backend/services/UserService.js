class UserService
{
    addUser(user)
    {
        // TODO: DB connection (add to DB)
        return true;
    }

    updateUser(user) {
        // TODO: DB connection (add to DB)
        return true;
    }

    deleteUser(user) {
        // TODO: DB connection (add to DB)
        return true;
    }

    getUser(user) {
        // TODO: DB connection (add to DB)

        // TODO: Return user model
        return true;
    }
}

module.exports = new UserService();